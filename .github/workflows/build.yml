name: Build

on:
  workflow_dispatch:
    inputs: {}
  push:
    branches: [ "develop" ]
    tags: [ "v**" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  sbt-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run tests
      run: sbt +test
  build-native:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build native app
        run: sbt GraalVMNativeImage/packageBin
      - name: Check native app (bifunctor)
        run: ./distage-example-bifunctor-tf/target/graalvm-native-image/leaderboard :help
      - name: Check native app (monofunctor)
        run: ./distage-example-monofunctor-tf/target/graalvm-native-image/leaderboard :help
      - name: Check native app (monomorphic)
        run: ./distage-example-monomorphic-cats/target/graalvm-native-image/leaderboard :help